version: '3'
services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    entrypoint: "composer update"
    tty: false
    working_dir: /usr/src/app
    volumes:
      - ./:/usr/src/app
      - vendor:/usr/src/app/vendor

volumes:
  vendor:
